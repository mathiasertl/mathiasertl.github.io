# mathiasertl.github.io

HTML source for https://er.tl - my personal little homepage.

## Build

```
uv sync
uv run python build.py
```

HTML is automatically published to the `gh-pages` branch
when pushing the ``main`` branch.
