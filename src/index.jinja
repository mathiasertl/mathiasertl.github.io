{% extends "_base.jinja" %}
{% set current_page = "home" %}

{% block title %}{{ site.name }}{% endblock %}

{% block main %}
<main class="max-w-2xl mx-auto px-6 pt-36 pb-24">

  <div class="mb-16">
    <!-- Avatar: replace the placeholder div with an <img> tag when you have a photo -->
    <div class="w-16 h-16 rounded-full bg-stone-200 mb-10 overflow-hidden flex items-center justify-center text-stone-400 text-xl select-none">
      {{ site.name[0] }}
    </div>

    <h1 class="text-3xl font-light text-stone-900 mb-3 tracking-tight">
      Hi, I'm <span class="font-medium">{{ site.name }}</span>.
    </h1>
    <p class="text-lg text-stone-500 font-light leading-relaxed">
      Software engineer &amp; occasional writer, based in [City].
    </p>
  </div>

  <!-- Intro paragraphs — edit freely -->
  <section class="mb-14 space-y-4 text-stone-600 leading-relaxed font-light">
    <p>
      I work on [topic / company / project]. I care deeply about [thing you
      value — e.g. open source, privacy, good design].
    </p>
    <p>
      When I'm not at a keyboard you'll find me [hobby], [hobby], or
      somewhere in the mountains.
    </p>
  </section>

  <!-- Quick-link pills -->
  <section class="flex flex-wrap gap-3">
    <a href="about.html"
       class="px-4 py-2 text-sm border border-stone-300 rounded-full text-stone-600 hover:border-stone-600 hover:text-stone-900 transition-colors">
      About me
    </a>
    <a href="links.html"
       class="px-4 py-2 text-sm border border-stone-300 rounded-full text-stone-600 hover:border-stone-600 hover:text-stone-900 transition-colors">
      Find me online
    </a>
    <a href="mailto:{{ site.email }}"
       class="px-4 py-2 text-sm border border-stone-300 rounded-full text-stone-600 hover:border-stone-600 hover:text-stone-900 transition-colors">
      Get in touch
    </a>
  </section>

</main>
{% endblock %}
